<div class="row">
	<div class="col-sm-10">
		<h1><%= @picture.title %></h1>
		<%= image_tag(@picture.image.url(:big)) %>
	</div>
</div>
<hr>
<% if user_signed_in? %>
	<%= render 'shared/comment' %>
<% end %>